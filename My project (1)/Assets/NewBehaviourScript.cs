using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using UnityEngine.UI;

public class NewBehaviourScript : MonoBehaviour
{
    // Deklaracja zmiennej na poziomie klasy
    public Button sendScoreButton; // Przycisk do wysyłania wyniku

    // Start is called before the first frame update
    void Start()
    {
        // Tutaj możesz umieścić kod, który ma być wykonany przy starcie gry
    }

    // Update is called once per frame
    void Update()
    {
        // Tutaj możesz umieścić kod, który ma być wykonany w każdej klatce
    }
}